<div class="container p-0 w-50 text-center ">
  <div class="row">
      <h2 class="mt-5 mb-lg-3 offset-lg-1">パスワード再設定</h2>
  </div>
  <div class="row">
      <p class="mt-3 mb-lg-3 offset-lg-1 text-left">
        ご登録時のメールアドレスを入力してください。<br>
        パスワード再発行用のメールをお送りします。
      </p>
  </div>
  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <div class="form-group row mt-3">
      <%= f.label :email, "Email", class:"col-lg-3 offset-lg-1 col-form-label text-left px-0" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control col-lg-6" %>
      <div class="form-group row mb-1">
        <% if resource.errors.messages[:email].present? %>
          <strong class="error-text"><%= resource.errors.full_messages_for(:email).first %></strong>
        <% end %>
      </div>
    </div>
    <div class="form-group text-right row">
      <%= f.submit "送信", class:"btn btn-primary mt-3 col-lg-3 offset-lg-7" %>
    </div>
  <% end %>
  <div class="row">
    <%= render "users/shared/links" %>
  </div>
</div>